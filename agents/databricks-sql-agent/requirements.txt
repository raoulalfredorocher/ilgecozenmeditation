# Databricks SQL Agent — dipendenze Python
# Installazione: pip install -r requirements.txt
# Su Databricks: %pip install -r requirements.txt

# --- Core Databricks ---
databricks-sdk>=0.20.0
databricks-sql-connector>=3.0.0

# --- LangChain + LangGraph (agente ReAct) ---
langchain>=0.2.0
langchain-community>=0.2.0
langgraph>=0.1.0

# --- MLflow (tracing, logging, deploy) ---
mlflow>=2.14.0

# --- Utility ---
pandas>=1.5.0
